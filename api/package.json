{
  "name": "couture-dynamic-pro-api",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "node --watch src/server.js",
    "start": "node src/server.js",
    "lint": "eslint src test",
    "typecheck": "echo \"api: JS simple, pas de typecheck (voir migration TS eventuelle)\"",
    "test": "node --test",
    "build": "echo \"api: pas de build, execution directe\""
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.2"
  },
  "devDependencies": {
    "eslint": "^8.57.0"
  }
}
