# Copier en .env.local — ne jamais committer .env.local (voir .gitignore).

NEXT_PUBLIC_API_URL=http://localhost:4000
# Sert à construire les URLs canoniques/OG et les liens de partage.
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Phase 7 — paiements côté client (clés publiques uniquement)
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=
